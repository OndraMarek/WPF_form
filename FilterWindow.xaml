<Window x:Class="BookWpf.FilterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookWpf"
        mc:Ignorable="d"
        Title="Filtrování" Height="128.148" Width="304.32">
    <Grid Margin="0,0,0,27">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="textBoxFilter" HorizontalAlignment="Stretch" Margin="135,13,23,0" Grid.ColumnSpan="2" />
        <Button x:Name="buttonFilterAuthor" Grid.Row="2" Content="Filtorvat" HorizontalAlignment="Center" Margin="84,10,108,10" Click="buttonOk_Click" Width="104" Grid.ColumnSpan="2" />
        <ComboBox x:Name="ComboBoxFilter" SelectedIndex="0" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Width="120">
            <ComboBoxItem>Autor</ComboBoxItem>
            <ComboBoxItem>Vydavatel</ComboBoxItem>
            <ComboBoxItem>Název</ComboBoxItem>
        </ComboBox>
    </Grid>
</Window>
